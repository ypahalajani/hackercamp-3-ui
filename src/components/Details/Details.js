import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``;

const Details = () => {
	return <Wrapper>I am Details Component</Wrapper>;
};

export default Details;
